export interface RoomsInterface {
    id: number
    photos: string[]
    room_type: string
    room_number:number
    description: string
    offer: boolean
    price: number
    discount: number
    cancellation: string
    ameneties: string[]
}